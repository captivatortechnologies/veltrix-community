apiVersion: v1
kind: Service
metadata:
  name: {{ include "veltrix.frontend.fullname" . }}
  labels:
    {{- include "veltrix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "veltrix.frontend.selectorLabels" . | nindent 4 }}
